
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace TalkSystem{

    public class PrologueController : MonoBehaviour
    {
        [SerializeField]
        private PrologueManager candyFace;
        [SerializeField]
        private PrologueManager FuFace;
        [SerializeField]
        private PrologueManager MashFace;
        [SerializeField]
        private PrologueManager KureFace;

        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }

}
